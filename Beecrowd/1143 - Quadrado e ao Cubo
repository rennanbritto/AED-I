#include <stdio.h>

int main() {
    int N, i, j, k;
    
    scanf("%d", &N);

    for(i = 1; i <= N; i++) {
        j = i * i; 
        k = j * i; 
        printf("%d %d %d\n", i, j, k);
    }
    
    return 0;
}
